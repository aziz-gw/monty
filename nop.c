#include "monty.h"
/**
 * nop - doesn’t do anything.
 * @stack: Double pointer to the top of the stack.
 * @line_number: Line number in the file where nop is called.
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
